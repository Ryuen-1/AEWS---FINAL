# MongoDB
MONGODB_URI=mongodb+srv://2301102644_db_user:<db_password>@cluster0.2rpkvnr.mongodb.net/?appName=Cluster0&compressors=zlib
MONGODB_DB=capstonesystem

SECRET_KEY=your-secret-key-change-in-production
PORT=8000

# Frontend URL for verification links (no trailing slash)
FRONTEND_URL=http://localhost:5173

# Gmail only – for verification/confirmation emails
# Use a Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
FROM_EMAIL=your-email@gmail.com

# reCAPTCHA v2 secret key (from https://www.google.com/recaptcha/admin, v2 "I'm not a robot" Checkbox).
# Must match the v2 site key used in the frontend. Do not use a v3 secret key here.
# RECAPTCHA_SECRET_KEY=your_reCAPTCHA_v2_secret_key

# AUTH TOKEN SECRET - Used to sign JWT tokens for authentication
# Generate a strong secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
# AUTH_TOKEN_SECRET=your-generated-secret-here-min-32-characters

# Redis Configuration (for email queue and background jobs)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your-redis-password
